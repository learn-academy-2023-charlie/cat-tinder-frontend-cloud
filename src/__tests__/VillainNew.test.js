import { render } from "@testing-library/react"
import VillainNew from "../pages/VillainNew"

describe("<VillainNew />", () => {
    it("renders without crashing", () => {})
        const div = document.createElement("div")
        render(<VillainNew />, div)
})
