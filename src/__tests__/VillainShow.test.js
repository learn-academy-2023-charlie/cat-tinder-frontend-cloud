import { render } from "@testing-library/react"
import VillainShow from "../pages/VillainShow"

describe("<VillainShow />", () => {
  it("renders without crashing", () => {})
    const div = document.createElement("div")
    render(<VillainShow />, div)
})
